smart-todo/
├── cmd/
│   └── api/
│       └── main.go          # Точка входа в приложение
├── internal/
│   ├── config/
│   │   └── config.go        # Загрузка конфигурации (порт, строка подключения к БД)
│   ├── database/
│   │   └── database.go      # Инициализация подключения к БД
│   ├── handler/
│   │   ├── auth.go          # Обработчики для /api/auth/*
│   │   ├── task.go          # Обработчики для /api/tasks/*
│   │   └── handler.go       # Общие функции для handlers (отправка ответов, ошибок)
│   ├── middleware/
│   │   ├── auth.go          # Middleware для проверки JWT
│   │   └── logger.go        # Middleware для логирования запросов
│   ├── model/
│   │   ├── user.go          # Структура User и методы
│   │   ├── task.go          # Структура Task и методы
│   │   └── request.go       # Структуры для входящих запросов (Register, Login)
│   ├── service/
│   │   ├── auth_service.go  # Логика регистрации, генерации токенов
│   │   └── task_service.go  # Логика работы с задачами (создание, валидация)
│   └── storage/
│       ├── storage.go       # Интерфейсы для работы с БД
│       ├── user_storage.go  # Реализация SQL-запросов для User
│       └── task_storage.go  # Реализация SQL-запросов для Task
├── migrations/
│   └── 001_init_db.sql      # SQL-файлы для создания таблиц
├── pkg/
│   └── utils/
│       └── utils.go         # Вспомогательные функции (хэширование пароля)
├── .env                     # Переменные окружения (НЕ коммитить в git!)
├── go.mod
└── go.sum